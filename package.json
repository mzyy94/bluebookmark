{
  "name": "bluebookmark",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "init:local": "wrangler d1 execute bluebookmark --local --file=src/database.sql",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "hono": "^4.0.0",
    "zod": "^3.22.4"
  }
}